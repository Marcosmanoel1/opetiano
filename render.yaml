services:
     - type: web
       name: opetiano
       runtime: python
       buildCommand: pip install -r requirements.txt
       startCommand: gunicorn app:app
       envVars:
         - key: WHATSAPP_TOKEN
           sync: false
         - key: PHONE_NUMBER_ID
           sync: false
         - key: VERIFY_TOKEN
           sync: false
